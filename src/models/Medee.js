class Medee {
  constructor(title, shortContent, imageUrl, category) {
    this.title = title;
    this.shortContent = shortContent;
    this.imageUrl = imageUrl;
    this.category = category;
  }
}

export default Medee;
